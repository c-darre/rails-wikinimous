<h1 class="mb-4">All Articles</h1>

<div class="list-group">
  <% @articles.each do |article| %>
    <%= link_to article.title, article_path(article), class: "list-group-item-action" %>
  <% end %>
</div>
